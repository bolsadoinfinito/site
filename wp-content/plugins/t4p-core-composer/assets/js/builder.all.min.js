
jQuery(document).ready(function(a) {
    var b = function(a) {
        a.show();
        var b = .75;
        var c = 500;
        var d = 100;
        var e = jQuery(".t4p-section-scroller", a);
        var f = jQuery(".t4p-section-scroller-inner", a)[0];
        e.on("wheel", function(a) {
            g(a.originalEvent.deltaY || a.originalEvent.deltaX);
            return false
        });
        function g(a) {
            if (a < 0) {
                a = -d
            } else {
                a = d
            }
            a = a * b;
            var c = e.find(".t4p-section-scroller-content").width();
            var g = e.width();
            if (c <= g) return false;
            var h = c - g + 10;
            a = parseInt(f.style.left || 0) - a;
            a = a >= 0 ? 0 : a;
            a = a <= -h ? -h : a;
            f.style.left = a + "px"
        }
        jQuery(".t4p-section-scroller-prev", a).click(function(a) {
            a.preventDefault();
            g(-c)
        });
        jQuery(".t4p-section-scroller-next", a).click(function(a) {
            a.preventDefault();
            g(c)
        });
        jQuery(window).load(function() {
            jQuery(window).resize(function() {
                g(0)
            })
        })
    };
    b(jQuery(".t4p-section.t4p-modules"));
    b(jQuery(".t4p-section.t4p-templates-load"))
});